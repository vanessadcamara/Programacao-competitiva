#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    int a[200123];
    cin >> n;
    for(int i = 0; i < n; i++){
        cin >> a[i];
    }
    set<int> st;
    st.insert(INT_MAX);
    for(int i = 0; i < n; i++){
        if(a[i] < pq.top()){
            
        }
    }
    return 0;
}